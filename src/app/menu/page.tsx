import { SectionIntro } from "@/components/section-intro";

export default function MenuPage() {
  return (
    <SectionIntro
      eyebrow="Plan ahead"
      title="Menu"
      description="This week, next week, and unplanned meals will live here."
    />
  );
}
