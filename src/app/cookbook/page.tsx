import { SectionIntro } from "@/components/section-intro";

export default function CookbookPage() {
  return (
    <SectionIntro
      eyebrow="Saved recipes"
      title="Cookbook"
      description="Your recipes, recipe books, and items needing review will live here."
    />
  );
}
