# Legacy Local VS Code Setup

This document is retained for historical reference only.

The old local OneDrive/Mac setup is no longer the active Big Al working environment.

## Current environment

Primary Codespaces working directory:

```text
/workspaces/big_al
```

Active branch:

```text
clean-milestone-4-sync
```

Use `AGENTS.md` for current Codex permissions and safety boundaries.

Use `docs/CURRENT_STATUS.md` and `docs/HANDOVER.md` for the current project state and resume point.

Do not rely on old instructions that prohibit routine milestone commits/pushes or that point Codex at the former local Mac folder.
